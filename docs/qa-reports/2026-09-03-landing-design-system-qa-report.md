# QA-отчёт: дизайн-система лендинга «Деловой Парк» (docs-only)

RUN_ID: `2026-09-03-landing-design-system`
PR: #834, ветка `claude/landing-design-system-github-gswrrt` → `main`, head `7b71d19`.

**Диапазон диффа.** Локальный `main` в этой рабочей копии отстаёт от `origin/main`
(на момент проверки `git fetch origin main` подтягивает уже смерженный коммит
`2048270` с этим же PR — дерево `2048270` байт-в-байт совпадает с деревом `7b71d19`,
`git diff 2048270 7b71d19` пуст). Это не меняет содержательную часть проверки:
все вычисления сделаны по `git diff origin/main...HEAD`, что и есть настоящий
дифф PR — 21 файл, `1724(+)/245(-)`, целиком под `design/`, `docs/product/`,
`docs/qa-reports/`, плюс `DESIGN.md`/`BRAND_CODE.md` (оба `M`). Кода приложения,
API, миграций, workflow в диффе нет — RBAC/rate-limit/API-кейсы из чек-листа
`agents/qa.md` неприменимы, отмечено явно в разделе Security ниже, а не
пропущено.

**Источник правды:** `docs/product/2026-09-03-landing-design-system-brief.md`,
раздел 7 «Критерии приёмки» = acceptance criteria. Раздел 8 брифа (внедрение в
код) вне области этого PR. Учтён `docs/qa-reports/2026-09-03-landing-design-system-review.md`
(code-reviewer, раунд 2, PASS) — ключевые числа (контраст, lint/tsc/test)
независимо пересчитаны и перепрогнаны заново, а не переписаны со слов ревью.

## Вердикт: PASS

Все 10 операционализированных проверок acceptance criteria (брифа §7) —
PASS, каждая подтверждена скриптом/grep, а не визуально. Регрессия
(`lint`/`tsc`/`test`) зелёная, `design/` не подключён к сборке. Security-кейсы
(в применимой для docs-only PR части) чисты. Есть 5 неблокирующих остаточных
наблюдений (раздел ниже) — ни одно не относится к явной формулировке AC из
брифа §7 и ни одно не меняет статус WCAG AA ни одной проверенной пары.

---

## AC → результат (брифа §7, через операционализацию задания)

| # | AC | Результат | Как проверял |
|---|----|-----------|---------------|
| 1 | В `tokens.json` нет hex в именах токенов (только в `$value`); JSON валиден; у каждого узла с `$value` есть `$type` | **PASS** | Скрипт-обход дерева (`node -e`): `JSON.parse` — OK; 87 узлов с `$value`, у всех есть `$type` (0 пропусков); grep по всем НЕ-`$`-ключам на hex-паттерн (`/#[0-9a-fA-F]{3,8}/`) — 0 совпадений (имена — kebab/dot: `surface.2`, `module.gazebos`, `status.warning` и т.п.) |
| 2 | Все перечисленные цветовые пары проходят WCAG AA (посчитано формулой relative luminance, не на глаз) | **PASS** | Свой скрипт (WCAG 2.1 relative luminance, независимый от расчётов в review-отчёте): `text.primary` на `surface.0/1` — 16.83:1 / 15.46:1; `text.secondary` на `surface.0/1` — 5.07:1 / 4.66:1; белый на `accent.default` — 4.70:1; белый на 5 `module.*` — `rental` 4.70, `gazebos` 5.02, `psPark` 5.70, `cafe` 5.18, `parking` 5.36 (все :1); белый на `status.warning`/`danger` — 5.02:1 / 4.83:1; `text.primary` на тинте 8% каждого из 5 модульных цветов поверх `surface.1` — 13.77–13.97:1. Все ≥4.5:1, все числа с точностью до сотых совпадают с пересчётом review-отчёта (кросс-проверка независимым скриптом). `text.tertiary` (`#86868b`, 3.62:1/3.33:1 на surface.0/1 — сам по себе не проходит 4.5:1, но токен документирует это и ограничивает применение ≥15px, что и проверяет следующий пункт) |
| 2b | `text.tertiary` только там, где текст ≥15px (кроме disabled/иконок) | **PASS** | `grep -n '#86868b' design/canvas/*.dc.html` — 12 вхождений. Реальный текст: placeholder-инпуты 15–16px (`Components.dc.html:85,101,107`; `Main.dc.html:120,121`; `Mobile.dc.html:78,79`) — ОК. `Components.dc.html:100` — 12px, но это явно `<span class="lbl">disabled</span>` (строка 99) — исключение "disabled". 4 вхождения — SVG-стрелки `stroke="#86868b"` (иконки, не текст) — исключение "иконок". `Components.dc.html:226` — SVG info-иконка 40px в empty-state отзывов, не текст; non-text contrast (WCAG 1.4.11, порог 3:1) 3.62:1 — проходит отдельно, но формально не "стрелка" (см. остаточное наблюдение №3). `Foundations.dc.html:53` — это цветовой свотч токена (документация самого токена, background/подпись `#86868b · только ≥15px`), не применение цвета к контентному тексту |
| 3 | В `Components.dc.html` — состояния default/hover/focus-visible/active/disabled для кнопок; default/focus/error/disabled для инпутов | **PASS** | Кнопки: `primary` (строки 41–45) и `secondary` (48–52) — по 5 `<div>` на все 5 состояний (default/hover/focus-visible/active/disabled, визуально отличимые: цвет / цвет темнее / box-shadow-кольцо / scale(0.98) / opacity 0.4); `ghost` (55–59) — аналогично 5. Инпуты (76–102): `default`(83–85)/`focus`(87–90, синяя рамка + ring)/`error`(92–96, красная рамка + сообщение)/`disabled`(98–101, серый фон) — все 4 присутствуют. `danger`-вариант кнопки (заявлен в брифе §4.1 как 4-й вариант) визуально не отрисован — см. остаточное наблюдение №1, не блокирует эту операционализацию AC (она про "состояния для кнопок" в целом, не про каждый вариант) |
| 4 | В `Mobile.dc.html`: CTA `height: 52px` и full-width (flex-колонка); бургер 44×44; текст нигде < 12px | **PASS** | `height: 52px` — 4 инстанса, все главные CTA: hero primary+secondary (37–39, `flex-direction: column` без `align-items`, дефолт `stretch`), waitlist submit (75–80, тот же паттерн), финальный CTA в Contacts (176–186, тот же паттерн) — все full-width через flex-stretch колонки. Бургер: `width: 44px; height: 44px;` (строка 23) — точное совпадение. `grep -o 'font-size: 1[01]px'` и `grep -o 'font-size: [0-9]px'` по всем 4 `.dc.html` — 0 совпадений каждый; минимальный реально используемый размер — 12px (полный список размеров: 12/13/14/15/16/17/18/20/22/24/28/32/40/48/56/64/96). Отдельно: карточные CTA "Забронировать"/"Заказать" в Services на Mobile — 48px, не 52px (см. остаточное наблюдение №2 — это не "CTA" из hero/waitlist/contacts, а вторичный `module`-вариант кнопки, ≥44px соблюдён) |
| 5 | `prefers-reduced-motion` в `tokens.json` и в `Foundations.dc.html` | **PASS** | `tokens.json`: `motion.reducedMotion.$value = "opacity-only 150ms"`, `$description` — "prefers-reduced-motion: без сдвигов и scale". `Foundations.dc.html:223`: `prefers-reduced-motion: всё → opacity 0→1 за 150ms, без сдвигов и scale" — согласовано дословно. Плюс (не требовалось, но проверил) — то же в `design/README.md:45` и `DESIGN.md:96` |
| 6 | Все 5 модулей имеют цвет в `color.module.*`; ни один не равен `accent.default`, кроме `rental` | **PASS** | `tokens.json`: `rental #0071e3`, `gazebos #15803d`, `psPark #7c3aed`, `cafe #c2410c`, `parking #0e7490` — все 5 присутствуют. `accent.default = #0071e3` совпадает только с `rental`; это не новое расхождение против прод — брифа §3.2 сам фиксирует, что в текущем коде `module.rental` уже равен `#0071e3` (аудит "сейчас в коде #0071e3, #16A34A, #7C3AED, ?, ?" — первое значение и есть rental), т.е. статус-кво, не новый визуальный отход, отдельного обоснования в списке изменений не требует |
| 7 | Шрифты только Manrope/Inter, `font-weight` ≤600 | **PASS** | `grep -o 'font-weight: 7[0-9][0-9]'` по всем артбордам — 0 совпадений; единственные применённые веса — `500` и `600` (плюс неявный браузерный default 400 для не-стилизованного текста). `font-family` только `Manrope, Inter, sans-serif` / `Inter, ...` (+ `-apple-system`/`system-ui` фолбэки) — единственное исключение `.mono` (`ui-monospace, "SF Mono", ...`), но проверил: класс используется только в `Foundations.dc.html`/`Components.dc.html` для подписей токенов (`color.*`, `radius.md` и т.п. — документация самой спецификации), в `Main.dc.html`/`Mobile.dc.html` (реальные мокапы страницы) `.mono` не встречается вообще (`grep 'class="mono"' Main.dc.html Mobile.dc.html` → 0) |
| 8 | Согласованность hex `color.*` между `tokens.json` ↔ `Foundations.dc.html` ↔ `DESIGN.md` ↔ `design/README.md`; старые `#16a34a`/`#ea580c`/`#0891b2`/`#d97706` не встречаются (кроме историчных упоминаний) | **PASS** | Скрипт: извлёк все 24 hex-значения `color.*` из `tokens.json` (без rgba) — все 24 присутствуют и в `Foundations.dc.html`, и в `DESIGN.md` (программная сверка, построчный вывод — 24/24 "true/true"). `design/README.md` не обязан перечислять все 24 (это выжимка "Ключевые решения", не полный словарь) — но там, где он приводит значения (12 из 24 — модули, статусы, secondary/tertiary, accent, surface.2), все совпадают. rgba-токены (`border.*`, `focus.ring`, `accent.subtle`) отдельно сверил построчно в `Foundations.dc.html` — присутствуют (формат без пробелов после запятых, `rgba(0,0,0,0.08)` вместо `rgba(0, 0, 0, 0.08)` — то же значение, не расхождение). Старые hex: `grep -rn` по `design/`+`DESIGN.md` — ровно 1 совпадение, `design/README.md:38`, в контексте "прежние `#16a34a`/... давали 3.2–3.7:1" — это разрешённое исключение (историческое упоминание) |
| 9 | Список изменений против прода присутствует | **PASS** | `canvas.json.annotations` — 3 блока: `diff-hero` (5 пунктов: secondary-текст, hover-серый, модульные цвета, лист ожидания, статусы/партнёры в токенах), `diff-mobile`, `diff-tokens`. `design/README.md` — секция "Ключевые решения (против прода на 2026-09-03)", 8 пунктов с конкретными было/стало значениями |
| 10 | `canvas.json`: все `artboards[].file` существуют; `w` = ширина корневого `<div>`; фреймы не перекрываются | **PASS** | Скрипт: все 4 файла (`Main`/`Mobile`/`Foundations`/`Components.dc.html`) существуют на диске. Корневой `<div style="width: ...">`: `Main` 1440 = заявлено 1440; `Mobile` 390 = 390; `Foundations` 1440 = 1440; `Components` 1440 = 1440 — все совпадают. AABB-проверка пересечений всех `C(4,2)=6` пар прямоугольников (`x,y,w,h` из `canvas.json`) — 0 пересечений (ближайшая пара, `Mobile`/`Foundations`, разнесена по x на 130px) |

---

## Security-чеклист (в применимой для docs-only части)

- **RBAC**: N/A. В диффе нет API-роутов, `auth()`/`hasModuleAccess` не затронуты — `git diff origin/main...HEAD --name-status` не содержит ни одного файла из `src/app/api/`, `src/proxy.ts`, `src/lib/permissions.ts`. Не выдумывал тест-кейсы под несуществующие эндпоинты.
- **Rate limiting**: N/A по той же причине — новых публичных эндпоинтов нет.
- **Input validation**: N/A — новых Zod-схем, форм с server-side обработкой нет (waitlist-форма в артбордах — картинка состояний, не код).
- **Data leakage**: `grep -rniE '(password|secret|NEXTAUTH|TELEGRAM_.*TOKEN|api[_-]?key|BEARER |sk-[a-zA-Z0-9]{10}|AKIA[0-9A-Z]{16}|-----BEGIN)'` по `design/`, `DESIGN.md`, `BRAND_CODE.md`, брифу — 1 совпадение, `BRAND_CODE.md:456` "**Password** — с кнопкой show/hide справа" (спецификация UI-поля инпута пароля, не значение секрета; строка не тронута этим PR — diff `BRAND_CODE.md` это только 6-строчный баннер "устарел" сверху файла, проверил `git diff origin/main...HEAD -- BRAND_CODE.md`). `.env*`/`*.pem`/`*.key`/`credentials*` — 0 в диффе.
  Публичные контакты в артбордах (`+7 (499) 677-48-88`, `@DelovoyPark_bot`) — сверил `grep` с живым `landing-delovoy-park.ru/components/contacts-section.tsx`: те же значения, уже публичные на проде, не утечка.
- **Изоляция от сборки**: `grep -rn "design/canvas" src/ next.config.ts public/` и то же для `design/tokens`/`design/README` — 0 совпадений везде. `design/` не влияет на рантайм до PR внедрения (§8 брифа), значит и рассуждение "а что если в артборде было бы что-то чувствительное" — не аргумент, оно физически недостижимо из собранного приложения.
- **Supply chain**: `package.json`/`package-lock.json` не в диффе — новых зависимостей нет.
- Инцидентов не найдено. Ни один security-кейс не FAIL → не блокирует вердикт (по правилу `agents/qa.md`, но здесь оно тривиально не применяется — блокировать нечему).

## Регрессия / сборка

- `npm run lint` → exit 0, **0 errors / 21 warnings**. Все warnings — в `src/components/messenger/*`, `src/modules/messenger/types.ts`, `src/modules/notifications/service.ts`, `src/modules/telephony/novofon-client.ts` (не части этого PR, унаследованы от базы ветки); ни одного упоминания `design/`/`DESIGN.md`/`BRAND_CODE` в выводе.
- `npx tsc --noEmit` → exit 0, пустой вывод.
- `npm test -- --run` → **317 test files / 4429 tests passed**, 0 failed (не переустанавливал `node_modules`, уже стояли). Совпадает с числами из review-отчёта раунда 2 — перепрогнал независимо, не поверил на слово.
- `design/` не подключён к сборке — см. Security выше.

## Edge cases (адаптировано под docs-only: нет рантайма, нет БД/HTTP)

- Пустые/невалидные данные, конкурентные запросы, превышение лимитов, 401/403/404 — N/A, эти классы edge case требуют исполняемого кода/API, которого в диффе нет.
- Эквивалент "граничных значений" для этого PR — контраст у нижней границы AA: минимальный найденный запас — `text.secondary` на `surface.1` (4.66:1 против требуемых 4.5:1, запас 0.16) — всё ещё PASS, но это самая тесная пара в системе, стоит иметь в виду при будущих правках `surface.1`/`text.secondary` в PR внедрения.
- Файловые граничные случаи (AC-10): все 4 артборда существуют, ни один не перекрывается — проверено программно, не "на глаз".
- Историческое/legacy-значение как false positive: старые hex (`#16a34a` и т.п.) действительно встречаются в репозитории — но ровно 1 раз и в разрешённом контексте (историческая ссылка), не как живое значение — проверено, не просто "grep нашёл 0", разобран единственный найденный случай.

---

## Остаточные наблюдения (не блокируют вердикт)

Не входят в явную формулировку 10 проверенных AC — фиксирую для полноты и для
PR внедрения (§8 брифа), аналогично тому, как их фиксировал code-reviewer в
своём раунде 2:

1. **`danger`-вариант кнопки** заявлен в брифе §4.1 (`primary`/`secondary`/`ghost`/`danger`) и текстово описан в `DESIGN.md:100-103`, но не отрисован в `Components.dc.html` — там есть только `primary`/`secondary`/`ghost`/`module` (полные 5-state матрицы) и отдельно цветовой свотч `status.danger` в `Foundations.dc.html:102` (не кнопка). Если он не нужен на публичном лендинге — стоит явно исключить из брифа §4.1 при следующей правке; если нужен (лендинг не использует, но примитив шарится с другими публичными страницами модулей per §4) — дорисовать в PR внедрения до того, как `src/components/ui/button.tsx` получит `variant="danger"` без визуального эталона.
2. **Высота кнопки `module` (`Забронировать`/`Заказать`/`Подробнее`) расходится между артбордами**: `Components.dc.html` (спецификация, строки 62–65) и `Main.dc.html` (десктоп, строки 141/148/159) рисуют её `height: 44px` (согласуется с заявленным в спецификации "height 44 md"), а `Mobile.dc.html` (строки 95, 102) — `height: 48px`. Обе величины ≥44px (touch-target AC-4 соблюдён в обоих случаях), но это единственное найденное расхождение спецификация↔мокап по числовому значению (не по цвету/тексту), достойное синхронизации в `design/canvas` до того, как это станет константой в коде.
3. **SVG-иконка (не текст) в `Components.dc.html:226`** использует `text.tertiary` на 40px круглой плашке (empty-state отзывов) — за пределами буквальной формулировки исключения "иконок-стрелок" (это info-иконка, не стрелка), но проходит отдельный порог WCAG 1.4.11 non-text contrast (3:1) с запасом (3.62:1). Не дефект, просто формулировка исключения в задании была уже, чем множество легитимных случаев в артбордах.
4. **Бейдж «Сдан» — alpha 90% поверх фото, не сплошная заливка** (`rgba(180,83,9,0.9)`, `Main.dc.html:75,84,93,102`, `Mobile.dc.html:57,65`). Сплошной `status.warning` даёт 5.02:1 (комфортный PASS), но худший теоретический случай композита поверх чисто белого участка фото — 4.21:1 (пересчитал независимо, совпадает с оценкой review-отчёта раунда 2). Непроверяемо точно без рендера реальных фото; сам бриф §3.5 оставляет этот класс проверки ("на реальных кадрах") на этап вёрстки. Не блокирует — просто риск на заметку для §8.
5. **Косметические неточности в самоописании контраста**: `tokens.json`/`DESIGN.md`/README называют `text.secondary` "5.2:1 на белом" (точный пересчёт — 5.07:1) и `text.tertiary` "3.5:1" (точный пересчёт — 3.62:1). Округления в разные стороны, но статус AA/non-AA ни у одной пары не меняется.

## Итог

10/10 операционализированных AC — PASS, каждый подтверждён скриптом или
воспроизводимым grep, не пересказом ревью. Независимый пересчёт контраста
(свой скрипт, не копия чужого) совпал с review-отчётом раунда 2 с точностью
до сотых — двойная перепроверка одним и тем же методом двумя разными
скриптами укрепляет уверенность в цифрах. `lint`/`tsc`/`test` зелёные,
`design/` изолирован от сборки. Security-кейсы, применимые к docs-only PR
(secrets, PII, изоляция от сборки, supply chain), чисты; RBAC/rate-limit/
input-validation — N/A, явно, а не пропущено. 5 остаточных наблюдений — все
неблокирующие, ни одно не относится к буквальной формулировке AC брифа §7 и
не меняет вывод WCAG AA ни по одной проверенной паре; переданы как заметки
для PR внедрения (§8 брифа), не как баг-репорты (баг-репорт по правилам
`agents/qa.md` предполагает воспроизводимый дефект против конкретного AC —
здесь ни один найденный пункт под это определение не подпадает).
