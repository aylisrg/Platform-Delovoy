# Review: fix(ci) — конвертер dependabot-singles падает на npm ci содержимого PR — issue #753

## Вердикт: PASS

## Acceptance Criteria
| AC | Статус | Комментарий |
|----|--------|-------------|
| AC-1 | PASS | `single-to-queue` теперь чекаутит `main`: `.github/workflows/dependabot-automerge.yml:66-69` — `actions/checkout@v7` получил `with: ref: main`, вместо дефолтного merge-ref PR на событии `pull_request`. Это ровно предложенный в issue фикс, применён к тому единственному шагу checkout в файле. |
| AC-2 | PASS | В файле ровно одна джоба — `single-to-queue` (`grep` по `^  [a-zA-Z_-]*:$` в файле находит только `pull_request:` (триггер) и `single-to-queue:` (джоба)). Других джоб на `pull_request`, которые чекаутили бы содержимое PR и гоняли по нему `npm ci` без нужды в этом содержимом, нет — AC тривиально выполнен, но я его проверил, а не принял на веру. |
| AC-3 | PASS | `git diff main..HEAD --stat` — один файл: `.github/workflows/dependabot-automerge.yml \| 10 ++++++++++`, `1 file changed, 10 insertions(+)`, без удалений. Все 10 добавленных строк — это `with: ref: main` (3 строки) плюс блок объясняющего комментария (7 строк) над тем же шагом. Ничего постороннего не тронуто. |
| AC-4 | PASS | `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/dependabot-automerge.yml'))"` — прошло без ошибок (`YAML OK`). `with:` вложен на правильном уровне под тем же шагом, что и существующий `if:` — синтаксис `actions/checkout@v7` валиден. |

## Scope Check
- Scope creep: Нет
- Лишние изменения: нет — единственный файл, единственный смысловой шаг (`with: ref: main`), плюс комментарий в духе уже принятого в файле стиля (в файле и так много развёрнутых объясняющих комментариев над каждым нетривиальным решением — новый вписывается в конвенцию, а не добавляет что-то чужеродное). Коммит один, сообщение `fix(ci): чекаутить main, а не merge-ref PR, в single-to-queue` соответствует conventional commits. Ветка `claude/issue-753-checkout-main-for-single-to-queue` соответствует конвенции `claude/issue-{номер}-{slug}`.

## Качество кода
- TypeScript strict: N/A (CI-workflow-only diff, TS-кода нет)
- Zod валидация: N/A
- API формат: N/A
- Тесты: OK — для YAML/CI-фикса юнит-тесты невозможны и не ожидаются; вместо этого:
  - `python3 -c "import yaml; yaml.safe_load(...)"` — валидность YAML подтверждена (перепроверено самостоятельно, см. AC-4);
  - заявлено и не переопровергнуто: `npm test && npx tsc --noEmit && npm run lint` — всё зелёное (4370/4370 тестов, tsc чисто, lint — только пре-существующие несвязанные warning'и, 0 ошибок). Отсутствие юнит-тестов для этого PR не является блокером.

## Безопасность
- Secrets leakage: `grep -riE '(password|token|secret|NEXTAUTH|TELEGRAM_.*TOKEN|api[_-]key)' .github/workflows/dependabot-automerge.yml` — совпадения есть только на уже существующих (не тронутых этим диффом) `${{ secrets.GITHUB_TOKEN }}` — это стандартный встроенный токен Actions, используется как и раньше через `env:`/`with:`, не логируется и не попадает в response. Новых секретов, хардкода токенов/паролей не добавлено.
- RBAC: N/A — это CI-workflow, не API endpoint.
- Supply chain: новых зависимостей не добавлено; `permissions:` блока (`contents: read`, `pull-requests: read`, `issues: write`) диффом не изменён — `ref: main` при `contents: read` только читает, ничего не пушит.
- Injection: изменение — статический литерал `ref: main`, никакого пользовательского ввода не участвует в этом шаге; инъекционной поверхности не добавлено. Остальной файл (использование `${{ steps.metadata.outputs.* }}` в `env:`, не в `run:`-интерполяции напрямую) диффом не тронут.
- Dangerous ops: нет `rm -rf`, `git push --force`, `git reset --hard`; миграций нет.
- Итог: инцидентов не найдено, security-чеклист пройден тривиально, как и ожидалось для CI-only диффа.

## Что хорошо
- Фикс — точное, минимальное попадание в issue: заменён ровно тот шаг checkout, который назван в issue, ровно тем YAML-сниппетом, который там предложен, без сопутствующего рефакторинга остального файла.
- Проверено дополнительное требование issue («заодно проверить, нет ли других джоб с тем же паттерном») — в файле объективно только одна джоба, что подтверждено грепом по структуре файла, а не пересказом issue.
- Новый комментарий над `ref: main` объясняет и «что», и «почему» (включая ссылку на конкретные PR #747/#748/#749, на которых баг воспроизводился) — соответствует уже сложившемуся в файле стилю документирования решений прямо в workflow.
