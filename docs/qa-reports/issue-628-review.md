# Review: `.mcp.json` playwright description — MCP interactive tool vs. E2E CI suite wording drift (#628)

## Вердикт: PASS

## Контекст

Issue #628 — та же формулировочная путаница, что уже была исправлена в `docs/mcp-servers.md`
для issue #574: описание `playwright` MCP-сервера в `.mcp.json` смешивало интерактивный
браузерный инструмент, которым агент пользуется внутри сессии (открыть страницу, заполнить
форму, свериться визуально), с полностью отдельным автоматическим E2E regression suite'ом
(`e2e/*.spec.ts`, `npm run e2e`, CI-job `E2E (Playwright)` из `.github/workflows/ci.yml`,
добавлен в #592) — CI-suite этот MCP-сервер вообще не использует.

Нет отдельного PRD/ADR/context для этой задачи (`docs/requirements|architecture|context/*628*`
— пусто), что ожидаемо: это точечный docs/config-фикс формулировки одного поля, эталон — уже
одобренная (issue #574) формулировка в `docs/mcp-servers.md`, а не новая архитектура.

## Acceptance Criteria (из текста issue #628 / инструкции ревью)

| AC | Статус | Комментарий |
|----|--------|-------------|
| Диф ограничен полем `description` секции `playwright` в `.mcp.json` | PASS | `git diff main...claude/issue-628-mcp-playwright-description --stat` → `.mcp.json \| 2 +-`; единственный hunk — строка 29 (было `"Browser automation for QA agent — E2E smoke tests, visual regressions."`) |
| `.mcp.json` остаётся валидным JSON после правки | PASS | `node -e "JSON.parse(require('fs').readFileSync('.mcp.json'))"` — без ошибок; файл целиком прочитан — структура (4 сервера, `$schema`) не повреждена |
| Новая формулировка корректно разделяет интерактивный MCP-инструмент и автоматический E2E suite | PASS | Новый текст явно называет обе стороны различия и даёт конкретные ссылки: `e2e/*.spec.ts`, `npm run e2e`, job `"E2E (Playwright)"`, `.github/workflows/ci.yml`, `#592` — и по смыслу совпадает с уже одобренной формулировкой в `docs/mcp-servers.md:63` (issue #574): «Интерактивная проверка в браузере во время сессии агента... Не путать с автоматическим regression-сьютом — тот живёт в `e2e/*.spec.ts`...» |
| Фактическая точность ссылок в новом тексте | PASS | `e2e/*.spec.ts` — файлы существуют (`e2e/admin-rbac.spec.ts`, `e2e/cafe-checkout.spec.ts`, и т.д.); `package.json:17` → `"e2e": "playwright test"`; `.github/workflows/ci.yml:114` → `name: E2E (Playwright)`; `git log` подтверждает — этот job добавлен коммитом `c5acfbb "feat(ci): Playwright E2E job — критические флоу против живого стека (#592)"`. Ссылка на `#592` не выдумана |
| Остальные 3 сервера (`postgres`, `filesystem`, `github-actions`) и `$schema` не тронуты | PASS | Полный файл прочитан построчно — совпадает с диффом: единственное изменение — строка 29 внутри блока `playwright` |
| Нет scope creep за пределы одного поля одного файла | PASS | `git diff main...HEAD --stat` — ровно `.mcp.json \| 2 +-`, 1 файл, 1 вставка/1 удаление |

## Scope Check
- Scope creep: Нет.
- Изменён ровно один файл (`.mcp.json`), ровно одно поле (`description` секции `playwright`).
- Никаких изменений в `docs/mcp-servers.md`, `package.json`, `.github/workflows/`, других
  секциях `.mcp.json`.

## Качество кода
- TypeScript strict / `any`: не применимо — изменение не в TS-коде, JSON-конфиг.
- Zod валидация / `apiResponse`/`apiError`: не применимо — нет API-логики в этом диффе.
- AuditLog: не применимо — не мутация данных.
- Валидность JSON: подтверждена явной проверкой парсером (см. AC выше) — это единственный
  риск для config-файла такого типа ("сломанный `.mcp.json` тихо ломает загрузку MCP-серверов
  для всех будущих сессий"), и он закрыт.

## Безопасность

### Secrets leakage
- `grep -rniE '(password|token|secret|nextauth|telegram_.*token|api[_-]key)' .mcp.json` — из
  реальных совпадений только `GITHUB_PERSONAL_ACCESS_TOKEN` в секции `github-actions.env`
  (переменная окружения `${GITHUB_PERSONAL_ACCESS_TOKEN}`, не литерал секрета) — эта секция
  диффом не тронута, дословно совпадает с версией на `main`.
- Новый текст `description` не содержит секретов, токенов, внутренних путей/ID.

### RBAC
- Не применимо — правка не затрагивает ни один API endpoint, роут или бизнес-логику.

### Supply chain
- Новых зависимостей нет; `package.json`/`package-lock.json` не изменены.

### Injection
- Не применимо — статическая строка описания в конфиге, не выполняется, не рендерится как HTML/SQL.

### Dangerous ops
- Нет деструктивных git/shell/DB операций в диффе.

**Инцидентов не найдено.**

## Тесты
- `npm test -- --run` (полный набор): **282 test files passed, 3946 tests passed**, 0 failed
  (изменение файла `.mcp.json` не затрагивает тестируемый код — ожидаемо без регрессий).
- Явный поиск тестов, ссылающихся на `.mcp.json` (`grep -r "mcp.json" **/*.test.ts`) — 0 файлов;
  автоматического теста на строку описания в MCP-конфиге в кодовой базе нет и быть не может
  (это не исполняемый код, а human/agent-readable метаданные).
- Отсутствие теста для этой задачи — не пробел, а ожидаемое состояние: аналогично тому, как был
  принят сиблинг-фикс #627 (CODEOWNERS docs) в этом же цикле ревью — оба случая: точечная правка
  текстового/конфигурационного поля без исполняемой логики, где приёмочный критерий проверяется
  чтением (валидность JSON + смысловое соответствие), а не юнит-тестом.

## Что хорошо
- Новая формулировка не просто "исправляет тон", а даёт конкретные, проверяемые якоря
  (путь `e2e/*.spec.ts`, команда `npm run e2e`, точное имя CI-job'ы, ссылка на issue #592,
  которая при проверке оказалась фактически верной) — снижает риск повторного дрейфа терминологии.
- Формулировка синхронизирована по смыслу с уже одобренным текстом в `docs/mcp-servers.md`
  (issue #574), что предотвращает расхождение между двумя источниками истины про этот MCP-сервер.
- Минимальный, хирургический дифф — ровно то, что просил issue, без сопутствующего рефакторинга
  соседних полей.

## Что исправить
Нет пунктов — вердикт PASS.
