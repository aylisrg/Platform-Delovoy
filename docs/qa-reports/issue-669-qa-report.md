# QA-отчёт: Issue #669 — architecture spike, недельный вид + drag-drop (US-5/US-6, эпик #442)

## Вердикт: PASS

## Контекст

- PR #739, ветка `claude/issues-442-15w98z`, единственный коммит `abb5971`
  (`git show --stat abb5971` подтверждает тот же SHA, что в задании), поверх
  `main` (`origin/main` актуален на момент проверки, `git fetch` выполнен).
- Это **architecture spike**, не реализация фичи: AC #669 требуют (1) ADR с
  явным решением по трём открытым архитектурным вопросам PRD и (2) заведённые
  в GitHub PR-размерные follow-up задачи на US-5/US-6. Кода/миграций/тестов в
  этом PR быть не должно и нет.
- `code-reviewer` уже дал PASS по PR (соответствие PRD, отсутствие scope
  creep, RBAC-корректность дизайна, фактическая точность ADR сверена с кодом)
  — эта проверка независимая, по AC #669, а не повтор code review.

## AC #669

| AC | Статус | Комментарий |
|----|--------|-------------|
| AC-1 | PASS | `docs/architecture/2026-08-23-booking-calendar-week-view-drag-drop-adr.md` (707 строк) даёт явное решение по всем открытым вопросам PRD 2–4 (в ADR разбиты на 4 секции, §2–§5, т.к. issue #669 объединял «источник данных» и «макет» в один пункт №1 — ADR решил их отдельно, это более подробно, а не расхождение с постановкой): §2 источник данных недельного вида (варианты A/B/C, выбран B — новый sibling-эндпоинт `week-timeline`, с количественным обоснованием: ~28 запросов в БД vs ~4, риск «рваной недели», расход rate-limit в 7×), §3 макет (варианты A/B/C, выбрана матрица «ресурс × день», с арифметическим обоснованием почему 7-колоночное расширение текущей процентной сетки физически нечитаемо — 105 часовых колонок), §4 общий компонент vs дублирование (варианты A/B/C, выбран C — общей становится только новая поверхность, с явным обоснованием через факт 1.1.4 о локализации модуль-специфики в пути создания брони), §5 техника drag-and-drop (варианты A/B, выбран гибрид B — droppable на строку ресурса + перевод `event.delta.x`, с формулами `snapped`/`newStart`/`newEnd` и таблицей реальных сложностей §5.3 с оценкой цены в строках кода). Ни один вопрос не закрыт заглушкой/«TBD» — по каждому расписаны минимум 2 отвергнутых альтернативы с конкретными техническими минусами (не общими фразами), не подтверждёнными на глаз: числа посчитаны из реального устройства кода (§1.1, 8 фактов, каждый со ссылкой на файл/строку — `getTimeline` `gazebos/service.ts:1856`, `ps-park/service.ts:1466`, индекс `schema.prisma:239`), а не предположений. |
| AC-2 | PASS | Issue #740 (US-5, `feat(gazebos,ps-park): недельный вид расписания в админ-календаре`) и issue #741 (US-6, `feat(gazebos): drag-and-drop переноса и растяжения брони в дневной сетке`) — обе реально существуют на GitHub (проверено `GET /repos/aylisrg/Platform-Delovoy/issues/{740,741}` через GitHub REST API с токеном сессии, не по телу PR), `state: open`, лейблы `prio:P2` + `auto:ready` (готовы к взятию автоочередью). Обе явно помечены как часть эпика #442 — в заголовке (`эпик #442`) и в теле (`Часть эпика #442. Результат architecture spike #669.`); формальной GitHub sub-issues-связи с #442 нет ни у одной из них, но это не регрессия и не гэп: у уже смерженных #665–#668 (US-1..US-4 того же эпика) `sub_issues_summary` для #442 тоже пуст — проект последовательно использует текстовую разметку заголовком/телом, а не нативную фичу GitHub sub-issues, и #740/#741 в точности следуют этому установленному паттерну. |

### Сверка скоупа issues #740/#741 против ADR §11 «Follow-up задачи»

Построчно сравнил тела issues с §11 ADR — расхождений не найдено:

- **Задача 1 / #740**: заголовок совпадает дословно
  (`feat(gazebos,ps-park): недельный вид расписания в админ-календаре (US-5, эпик #442)`).
  «Скоуп (входит)» issue перечисляет ровно те 4 пункта ADR (§11): `week-timeline.ts`
  + типы + тесты, `weekTimelineQuerySchema`, два тонких роута с RBAC/rate-limit,
  `week-schedule-grid.tsx` + переключатель «День/Неделя». «Не входит» issue
  дословно повторяет список ADR (месяц, мобильный недельный вид, inline-создание,
  печать недели, рефакторинг дневной сетки, изменение `GazeboResource`, БД).
  AC issue (AC-1..AC-6) — это ровно AC US-5 из PRD (сверено построчно с
  `docs/requirements/2026-08-16-booking-calendar-operator-ux-prd.md:186-191`).
  «Зависимости: нет» — совпадает с ADR §11 и §5.5.
- **Задача 2 / #741**: заголовок совпадает дословно
  (`feat(gazebos): drag-and-drop переноса и растяжения брони в дневной сетке (US-6, эпик #442)`).
  «Что сделать» issue дословно повторяет технику ADR §5 (`timeline-drag.ts`,
  `DndContext`+`PointerSensor{distance:8}`+`DragOverlay`, `useDroppable` на
  строку ресурса, no-op при нулевом смещении, блокировка in-flight). «Не
  входит» issue повторяет ADR §10/§11 (Плей Парк, мобильный, drag в неделе,
  bulk, любые серверные изменения). AC issue (AC-1..AC-7) — ровно AC US-6 из
  PRD (`docs/requirements/...prd.md:208-214`). «Зависимости: организационно
  после US-5» — совпадает с ADR §5.5/§11.

Слова ADR и реально заведённые issues не разошлись.

## npm test / tsc — регрессия

```
npm test -- --run   → Test Files 312 passed (312), Tests 4342 passed (4342)
npx tsc --noEmit    → чисто, пустой вывод, exit 0
```

Числа (312 файлов / 4342 теста) совпадают с ожидаемым базовым прогоном из
задания — регрессий нет, что ожидаемо: PR не меняет ни одного файла с кодом.

## PR mergeability

- `git diff origin/main...claude/issues-442-15w98z --stat` — ровно 2 файла,
  `811(+) 0(-)`: новый ADR (707 строк) + append к context-файлу (104 строки).
  Никакого кода, тестов, `package.json`, `prisma/schema.prisma` — не тронуты.
  `git diff ... | grep -inE "password|secret|token|api[_-]?key|NEXTAUTH|PRIVATE"` —
  пусто, утечек в диффе нет.
- Тело PR #739 содержит `Closes #669` (подтверждено через GitHub API,
  `pulls/739`), коммит `abb5971` тоже содержит `Closes #669` в сообщении.
- `base: main`, `head: claude/issues-442-15w98z` @ `abb5971ba08a3c36a69a66b5e6c0c16795566be0` —
  совпадает с заданием.
- `mergeable: true`, `mergeable_state: clean` (GitHub API).
- Все 8 значащих CI-чеков на `abb5971` зелёные: Build Docker Image, Test,
  Security Audit, E2E (Playwright), Lint, Lockfile in sync, AC-трассируемость,
  TypeScript Check — все `completed / success`.
- PR открыт как `draft: true`. Это не блокер: по конвенции очереди
  (`scripts/lib/issue-queue.ts`, `autoMergeSkipReason`) черновик — не признак
  незавершённости, а обязательное поведение веб-сессий Claude Code; готовность
  определяют гейт/CI/вердикты ревью-агентов, а не флаг `draft` — свипер снимает
  его сам, когда всё остальное сошлось.
- Единственное расхождение с чек-листом PR: чекбоксы «Тип изменения» и
  «Затронутые модули» не отмечены (`[x]` только у `chore:`/`docs:` и у
  gazebos/ps-park — верно), но пункты «Добавлены тесты для новой логики» и
  API/Zod/AuditLog пункты чек-листа оставлены `[ ]` с пояснением «не
  применимо — только ADR» рядом — корректно, т.к. кода нет. Не является
  дефектом.

## Итоговое состояние эпика #442

Проверено запросами к GitHub API (`state=all`), не на слово телу PR:

| Story | Issue | Состояние |
|---|---|---|
| US-1 | #665 | `closed` (смержен PR #672) |
| US-2 | #666 | `closed` (смержен PR #673) |
| US-3 | #667 | `closed` (смержен PR #676) |
| US-4 | #668 | `closed` (смержен PR #677) |
| spike | #669 | `open`, закрывается этим PR #739 |
| US-5 | #740 | `open`, `prio:P2` + `auto:ready` — готова к взятию |
| US-6 | #741 | `open`, `prio:P2` + `auto:ready` — готова к взятию |

Дополнительно проверено, что среди issues репозитория (список `state=all` за
последний месяц) нет других открытых задач, ссылающихся на «эпик #442»,
кроме перечисленных выше и самого эпика #442 (`open`, разметочная сводная
задача, `sub_issues_summary` пуст — эпик заведён до появления практики
формальных sub-issues в проекте, дочерние задачи связаны текстом заголовков,
как и у #665–#668). После мержа PR #739 (закрывает #669) у эпика не остаётся
неучтённых пунктов из шести реальных user story: US-1..US-4 доставлены,
US-5/US-6 имеют готовые к взятию PR-размерные задачи.

## Security

PR не меняет код — стандартные функциональные security-кейсы (RBAC на
эндпоинтах, rate limiting, input validation, data leakage) неприменимы к
самому PR: новых endpoint'ов, форм, полей ввода не появляется. Проверено
дополнительно: диффа на секреты/токены/пароли — чисто (см. выше). RBAC-дизайн
для *будущих* эндпоинтов (`week-timeline`) в ADR §7.3 сам по себе описан
корректно (`requireAdminSection`, а не `hasModuleAccess`, с явным обоснованием
почему), но это предмет проверки для будущих PR #740/#741, не для этого PR —
здесь нет кода, который можно было бы прогнать через RBAC-тесты.

## Итог

- AC #669: 2 из 2 PASS.
- `npm test`: 4342/4342 (312 файлов), `tsc --noEmit`: чисто — регрессий нет
  (ожидаемо, код не менялся).
- PR: диф ровно 2 файла документации, `Closes #669` присутствует, ветка/SHA
  совпадают с заданием, все CI-чеки зелёные, `mergeable_state: clean`.
- Эпик #442: после мержа этого PR US-1..US-4 смержены, US-5/US-6 имеют
  готовые к взятию задачи (#740, #741) — неучтённых story не остаётся.
- Слова ADR §11 и реально заведённые issues #740/#741 сверены построчно —
  расхождений нет.

**Вердикт: PASS.** PR #739 закрывает issue #669 по обоим AC: ADR даёт
обоснованное решение (не заглушку) по всем открытым архитектурным вопросам
PRD, follow-up задачи #740/#741 заведены, открыты, размечены как часть эпика
#442 и по объёму точно соответствуют тому, что обещает ADR. Готов к автомержу
(`code-reviewer` уже PASS, теперь и `qa-engineer` PASS).
