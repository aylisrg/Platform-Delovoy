# QA-отчёт: Issue #575 — qa-patterns.md: содержательные паттерны вместо пустых секций

## Вердикт: PASS

## Контекст
- Ветка: `claude/issue-575-qa-patterns`, HEAD `46924c9`, 2 коммита поверх `main` (`063177b` —
  основной чендж, `46924c9` — однострочный фикс опечатки после NEEDS_CHANGES от
  `code-reviewer`, повторное ревью дало PASS).
- PRD в `docs/requirements/` отсутствует — chore-задача над промпт/доксами конвейера, AC взяты
  прямо из формулировки issue, консистентно с прецедентами #574/#564 (тоже docs/config-only, тоже
  без PRD).
- `git diff main...HEAD --stat`: **3 файла**, `41 insertions(+), 6 deletions(-)`:
  - `.claude/agents/qa-engineer.md` (+4/-1)
  - `.claude/feedback/qa-patterns.md` (+17/-6)
  - `agents/qa.md` (+26)
- Кода приложения дифф не касается. `scripts/collect-qa-feedback.sh` — намеренно не тронут
  (issue прямо требует оставить его statistics-only) — подтверждено: `git diff main...HEAD --
  scripts/collect-qa-feedback.sh` пуст.

## Acceptance Criteria

| # | AC | Статус | Комментарий |
|---|----|--------|-------------|
| 1 | Секции «Частые ошибки» непустые (реальные паттерны из истории QA-отчётов) | PASS | Все 5 секций (`TypeScript / Качество кода`, `API / Валидация`, `Тесты`, `RBAC / Безопасность`, `Scope Creep`) заполнены, суммарно 8 строк-паттернов вместо HTML-комментариев-заглушек. См. независимую проверку ниже — 7 из 8 паттернов дословно подтверждены построчным сравнением с реальными `docs/qa-reports/*.md`, 1 частично (см. «Замечания»). |
| 2 | Промпты QA-агента содержат шаг дозаписи с форматом и правилом ротации | PASS | И `agents/qa.md` (новый раздел «После вердикта: обновление qa-patterns.md»), и `.claude/agents/qa-engineer.md` (новый абзац после шага про `collect-qa-feedback.sh`) содержат: формат `- Паттерн → как избегать (issue #NNN).`, условие «только новый, ранее не описанный паттерн», и правило ротации, явно описанное как ручной шаг QA-агента (не автоматика скрипта). Оба файла и сам `qa-patterns.md` согласованы по порогу ротации (≥ ~20 строк на секцию / ≥ ~100 строк в файле в целом). |
| 3 | `npm run agents:eval` зелёный | PASS | `✅ [po] / [architect] / [reviewer] / [qa] sample-gazebo-payment` — **4/4 passed, 0 failed**. |

## Независимая проверка содержимого паттернов (не доверяя описанию задачи)

Прочитал каждую из 8 новых строк и сверил цитируемые issue-номера с первоисточником —
построчным чтением реальных `docs/qa-reports/*.md` (а для #564/#574, чьи коммиты не влиты в
`main`, — через `git show <commit>:docs/qa-reports/issue-NNN-qa-report.md` на их родных ветках,
`git log --all` подтверждает существование этих коммитов и веток):

- **#543 (NaN/Infinity → `Number.isFinite`)** — подтверждено дословно:
  `2026-08-14-issue-543-penalty-nan-infinity-qa-report.md` содержит именно `Number.isFinite`,
  generic-лейбл вместо «NaN ₽»/«Infinity ₽».
- **#432 (единая Zod-схема на весь body)** — подтверждено: `2026-08-12-issue-432-patch-booking-zod-qa-report.md` явно про `safeParse()` всего тела.
- **#438 (`isPublicApiRoute` матчит по префиксу модуля)** — подтверждено дословно, вплоть до
  номеров строк: `2026-08-13-issue-438-booking-history-search-qa-report.md`, строка 89 —
  `startsWith("/api/gazebos")`/`startsWith("/api/ps-park")` в `auth.config.ts`.
- **#564/#622 (mutation-check, «6 уронил 6» / «5 уронил 5»)** — оба числа подтверждены дословно:
  `issue-564-qa-report.md` (ветка `claude/issue-564-deletedat-filters`, ещё не влита в `main`) —
  «6 failed | 2 passed», ровно 6 новых тестов; `issue-622-qa-report.md` — «Test Files 5 failed
  (5) / Tests 5 failed | 32 passed (37)», ровно 5. Это же число (5, не 6) в тексте паттерна RBAC
  ниже — согласовано с фиксом опечатки в `46924c9`.
- **#560/#561/#622 (`requireAdminSection` после `hasRole`, 5 write-роутов в #622)** — подтверждено:
  `issue-622-qa-report.md` — заголовок и AC-таблица прямо называют «5 write-роутов» (`admin-book`
  ×2, `extend`, `pay-online`, `add-items`), паттерн вызова `hasRole → requireAdminSection`
  идентичен во всех трёх отчётах (#560, #561, #622).
- **#423/#489/#512/#557/#564 (`deletedAt: null` пропущен в мутирующих lookup'ах)** — все 5
  подтверждены: #423 (13 мест в `gazebos/service.ts`), #489/#557 (health-роуты gazebos/ps-park),
  #512 (`checkInBooking` ps-park), #564 (ещё 6 мест в `ps-park/service.ts`, ветка не влита, но
  коммит и QA-отчёт реальны — см. выше).
- **#471/#534 (пользовательский текст в `parse_mode:"HTML"` без `escapeHtml`)** — подтверждено:
  оба отчёта явно про интерполяцию `problemNote`/`managerName`/имён в Telegram-сообщения без
  экранирования, канонический хелпер `src/lib/telegram/escape.ts`.
- **#625/#627/#628 (сосед. баг того же класса → отдельная issue, не в этом PR)** — **частично
  подтверждено**. `issue-574-qa-report.md` (ветка `claude/issue-574-docs-sync`, не влита в
  `main`) прямо называет #627 и #628 как issues, заведённые вместо расширения скоупа
  (`CODEOWNERS`, `.mcp.json`). Issue **#625** нигде в локально доступных отчётах/ветках/коммитах
  не встречается (`git log --all`, `grep -r` по `docs/` — 0 совпадений вне самого
  `qa-patterns.md`) — без доступа к GitHub API (в этом окружении `gh` не установлен, MCP-инструмент
  не подключён) не могу независимо подтвердить, что #625 существует и относится именно к этому
  классу находок. Не считаю это фатальным для AC-1: паттерн как таковой (не чинить второй баг
  в том же PR) реален и дважды подтверждён (#627, #628), а #625 правдоподобен по контексту
  («после ревью #564/#574» — у #564 действительно был code-reviewer, независимо проверивший все
  20 call sites, что типично порождает побочные находки) — но это не строгая верификация,
  фиксирую как замечание ниже, не как блокер.

## Регрессия
- `npm test -- --run`: **268 test files passed (268), 3802 tests passed (3802)**, 0 failed.
- `npx tsc --noEmit`: чисто, без ошибок.
- `npm run agents:eval`: **4/4 passed, 0 failed** (po, architect, reviewer, qa — все sample-кейсы).

## Scope check
- `git diff main...HEAD --name-only`: ровно 3 файла — `.claude/agents/qa-engineer.md`,
  `.claude/feedback/qa-patterns.md`, `agents/qa.md`. Кода приложения, схемы БД, workflow'ов —
  не тронуто.
- `scripts/collect-qa-feedback.sh` подтверждённо не тронут (`git diff main...HEAD --
  scripts/collect-qa-feedback.sh` — пустой вывод) — соответствует явному требованию issue
  оставить его statistics-only.
- Секция «Статистика» в `qa-patterns.md` (таблица дат/отчётов/счётчиков) не изменена этим PR —
  диф ограничен ровно секцией «Частые ошибки» и новым пояснительным блоком над ней.

## Security-чеклист (функциональный)
Промпт/докс-only PR, не затрагивает API/RBAC/rate limiting/пользовательские данные — функциональные
security-кейсы (RBAC, rate limiting, input validation, data leakage) неприменимы, аналогично
прецеденту #574. Отдельно проверено: секреты/токены в диффе отсутствуют
(`grep -rniE '(password|token|secret|nextauth|telegram_.*token|api[_-]key)'` по всем трём файлам —
0 совпадений), никаких prompt-injection или инструкций на обход `agents/SECURITY.md`/merge-гейта
в добавленном тексте не обнаружено — обычные документационные правки, соответствуют issue.

## Замечания (non-blocking)
1. Один из 8 паттернов (Scope Creep) ссылается на issue #625, которую не удалось независимо
   подтвердить локально (нет коммита/ветки/упоминания вне самого `qa-patterns.md`, а `gh`/GitHub
   MCP в этом окружении недоступны). Остальные 7 issue-ссылок в этой же строке и во всех прочих
   7 паттернах — подтверждены дословно. Не блокирует вердикт: сам паттерн верный и дважды
   подтверждён другими номерами, доступ к проверке #625 — ограничение окружения, а не признак
   фабрикации (стиль и контекст полностью согласуются с реальными находками #627/#628 из того же
   предложения).
2. Формат части строк — не буквально `(issue #NNN)`, а `(issues #NNN, #NNN — <пояснение>)` для
   паттернов с несколькими прецедентами (Тесты, RBAC ×2, Scope Creep). Это осознанное и полезное
   расширение согласованного формата (сохраняет ссылочную трассируемость к каждому issue), не
   противоречие AC-2 — не блокер.

## Итог
- Всего AC: 3
- PASS: 3
- FAIL: 0
- Security-кейсы: неприменимы (docs/prompt-only), явных нарушений не найдено
- `npm test` (268/268, 3802/3802), `tsc --noEmit`, `npm run agents:eval` (4/4) — все чисто
- Каждая из 8 новых строк паттернов проверена против первоисточника; 7/8 подтверждены дословно
  построчным сравнением, 1/8 частично (issue #625 в тексте паттерна Scope Creep — см. «Замечания»)
- Инструкции в `agents/qa.md` и `.claude/agents/qa-engineer.md` взаимно согласованы по формату и
  правилу ротации, явно описанному как ручной шаг QA-агента
- Скоуп строго ограничен 3 промпт/доксами, `collect-qa-feedback.sh` подтверждённо не тронут

**Вердикт: PASS.** Все три acceptance criteria выполнены: секции «Частые ошибки» заполнены
содержательными, преимущественно верифицированными паттернами (не generic filler), промпты
QA-агента (`agents/qa.md` + `.claude/agents/qa-engineer.md`) содержат согласованную инструкцию
по дозаписи с форматом и явно ручным правилом ротации, `npm run agents:eval` зелёный. Полный
прогон тестов и типов чист, `collect-qa-feedback.sh` не тронут, скоуп не расширен за пределы трёх
заявленных файлов. Единственное замечание (issue #625 не подтверждена независимо в этом
окружении) не блокирует вердикт — не найдено признаков фабрикации, только ограничение доступа к
GitHub API при проверке.
