# Чек-лист дня перезапуска бронирования (беседки + PS Park)

Дата составления: 2026-08-10. Выполняется владельцем/старшим админом ПОСЛЕ мержа и деплоя всех P0-фиксов (см. `docs/product/2026-08-10-booking-relaunch-plan.md`).

## Предусловия
- [ ] Все P0-issues закрыты, PR смержены и задеплоены (blue-green деплой прошёл, `/api/health` зелёный).
- [ ] `npm test` зелёный на main.
- [ ] Администраторы прочитали `docs/runbooks/booking-operator-guide.md`, вопросы разобраны.

## Подготовка (за день)
- [ ] Сверить цены и ресурсы: 5 беседок (прайс от 28.04.2026, `metadata.priceList`), 4 стола PS Park — в `/admin/gazebos/resources` и `/admin/ps-park/resources`.
- [ ] Сверить телефон для CTA на публичной странице (`scripts/set-public-phone.ts` / настройки).
- [ ] Проверить, что доступы админов выданы (роль MANAGER + доступ к разделам gazebos / ps-park).
- [ ] Убедиться, что cron-контуры живы: no-show, overdue-reminders, payments-reconcile (последние запуски без ошибок в SystemEvent). ⚠️ Открыт issue #371: middleware блокирует анонимные `/api/cron/*` — до его закрытия авто-no-show и напоминания могут не работать; проверить фактическое прохождение кронов обязательно.

## Включение Telegram
- [ ] `/admin/monitoring`: включить `telegramAdminChatEnabled` и `telegramChannelEnabled` для `gazebos` и `ps-park` (chat ID сохранены с момента отключения).
- [ ] Проверить, что workflow `disable-gazebos-pspark-channel.yml` никем не запускается повторно.
- [ ] Тест-кнопки каналов в настройках обоих модулей — сообщение дошло в оба канала.

## Google Calendar
- [ ] Создать тестовую админ-бронь беседки → событие появилось в календаре беседки.
- [ ] Отменить её → событие удалилось.

## Флип
- [ ] `/admin/gazebos/settings` → включить «Публичное бронирование» (`publicBookingEnabled = true`).

## Сквозной смоук (сразу после флипа)
- [ ] С телефона (мобильный интернет, не Wi-Fi парка): открыть /gazebos, создать бронь с онлайн-оплатой (тест по чек-листу `docs/qa-reports/2026-05-04-wave-1-manual-test-checklist.md`).
- [ ] Оплата прошла → бронь стала «Подтверждена», уведомление `booking.paid` пришло в канал.
- [ ] Админ видит бронь в таймлайне; «Завершить» со счётом работает из таймлайна (P0-3).
- [ ] Отмена через личный кабинет/webapp: статус «Отменена», слот освободился, событие GCal удалено, уведомление пришло (P0-4).
- [ ] Бронь по телефону: квик-форма → появилась в календаре и GCal.
- [ ] PS Park: открыть смену, создать бронь, завершить со счётом, закрыть смену — суммы сходятся.

## Мониторинг после запуска (48 часов)
- [ ] `/api/gazebos/health`, `/api/ps-park/health` — без 5xx-спайков.
- [ ] SystemEvent: нет ERROR/CRITICAL от rate-limit/booking/payments.
- [ ] Ежедневно: сверка выручки в `/admin/gazebos/analytics` с фактической кассой смены.

## Откат (если критический сбой)
1. `/admin/gazebos/settings` → выключить публичное бронирование (публичная страница вернётся к «позвоните нам»; админ-брони продолжают работать).
2. При проблеме с Telegram-спамом: `/admin/monitoring` → выключить каналы модулей.
3. Зафиксировать инцидент в `docs/incidents/`, завести issue.
