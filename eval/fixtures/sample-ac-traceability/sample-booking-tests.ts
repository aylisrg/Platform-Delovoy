import { describe, it } from "vitest";

// Фикстура для eval/__tests__/traceability.test.ts — читается как текст, не
// исполняется сама (имя намеренно без суффикса `.test.ts`/`.spec.ts`, иначе
// Vitest подхватил бы её как реальный тестовый файл проекта — тот же класс
// коллизии с именем/glob'ом, что уже ловил QA на issue #582). Демонстрирует
// оба вида маркера трассируемости. Третий критерий из фикстуры-PRD намеренно
// оставлен непокрытым — показывает, как чекер репортит частичное покрытие.
// (Ссылка на его номер намеренно не приведена в этом абзаце — иначе строка
// сама стала бы ложным маркером для чекера, который её же и тестирует.)
describe("booking payment flow (fixture)", () => {
  // AC-1: кнопка "Оплатить" видна после выбора даты
  it("shows the pay button once a date is selected", () => {});

  it("marks booking CONFIRMED after successful payment (AC-2)", () => {});
});
