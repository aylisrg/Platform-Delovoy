{
  "$schema": "https://json.schemastore.org/claude-code-mcp.json",
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "${DATABASE_URL}"
      ],
      "env": {},
      "description": "Read-only Postgres access for Architect/Analyst agents. Schema exploration, query generation."
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ],
      "description": "Scoped filesystem access to project root only."
    },
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-playwright"
      ],
      "description": "Interactive browser checks during an agent session (open a page, fill a form, visually verify) — not the automated E2E regression suite, which lives in e2e/*.spec.ts (npm run e2e) and runs in CI as the separate \"E2E (Playwright)\" job (.github/workflows/ci.yml, added in #592) without this MCP server."
    },
    "github-actions": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Расширенный GitHub MCP с actions:write scope. Параллельно с базовым harness-MCP — даёт workflow_dispatch для timeweb-manage.yml (server-status, server-logs, disk-cleanup), trigger deploy.yml, etc. Tools под namespace mcp__github-actions__*. Требует PAT в env (см. AGENTS.md → MCP servers)."
    }
  }
}
